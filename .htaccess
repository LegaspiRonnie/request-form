<IfModule mod_rewrite.c>
  RewriteEngine On

  # If the requested resource doesn't exist, serve the SPA entry.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>

